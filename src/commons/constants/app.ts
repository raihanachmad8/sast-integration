/**
 * Application-wide constants — name, version, and feature flags.
 */

export const APP_NAME = 'SAST Integration';
export const APP_VERSION = '0.4.0';
export const APP_BASE_URL = process.env.NEXT_PUBLIC_APP_URL || 'https://sast.local';
